export function init({ layers } /*, options = {}*/) {
  if (!layers) {
    return;
  }
  // does nothing for now, but can be extended in the future
}

export function reset({ layers } /*, options = {}*/) {
  if (!layers) {
    return;
  }
  // does nothing for now, but can be extended in the future
}
